#include "main.h"

/**
 * add - a function that adds two integers and returns the result.
 * @a: an iteger representing the firs operand
 * @b: an iteger representing the second operand
 * return: a + b sum tow numbers.
 */

int add(int a, int b)
{
	return (a + b);
}
